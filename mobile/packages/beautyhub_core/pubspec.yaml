name: beautyhub_core
description: SDK partagé BeautyHub — bootstrap marque blanche, thème, API.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.10.9
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  supabase_flutter: ^2.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
